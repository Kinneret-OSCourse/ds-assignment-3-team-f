# Assignment 3 computer 1: PostgreSQL node 1.
#
# Run on DB computer 1:
#   docker compose --env-file .env -f docker-compose-db1.yml --profile db1 up -d --build

services:
  postgres-node1:
    extends:
      file: docker-compose.12-computers.yml
      service: postgres-node1
    profiles: ["db1"]

volumes:
  postgres_node1_data:
