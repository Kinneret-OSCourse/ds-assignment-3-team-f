# Assignment 3 computer 3: PostgreSQL node 3.
#
# Run on DB computer 3:
#   docker compose --env-file .env -f docker-compose-db3.yml --profile db3 up -d --build

services:
  postgres-node3:
    extends:
      file: docker-compose.12-computers.yml
      service: postgres-node3
    profiles: ["db3"]

volumes:
  postgres_node3_data:
