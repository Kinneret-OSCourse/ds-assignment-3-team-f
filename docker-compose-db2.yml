# Assignment 3 computer 2: PostgreSQL node 2.
#
# Run on DB computer 2:
#   docker compose --env-file .env -f docker-compose-db2.yml --profile db2 up -d --build

services:
  postgres-node2:
    extends:
      file: docker-compose.12-computers.yml
      service: postgres-node2
    profiles: ["db2"]

volumes:
  postgres_node2_data:
